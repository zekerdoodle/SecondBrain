name: your_agent_name
type: sdk                    # sdk | cli
model: sonnet               # sonnet | opus | haiku
description: "Brief description of what this agent does."
chattable: false             # true to show in chat dropdown
color: "#6C7086"
icon: bot                    # heart, zap, brain, search, globe, code, layers, sparkles, bot
# system_prompt_preset: claude_code  # Use Claude Code's native system prompt; prompt.md becomes append content
tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
  # Add more tools as needed. MCP tools use prefix: mcp__brain__tool_name
timeout_seconds: 300
